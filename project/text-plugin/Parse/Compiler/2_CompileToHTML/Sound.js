import _ from 'underscore'
import { getPreviousID } from 'Parse/Compiler/1_Precompile/UpdateID'
// import store from 'App/store'

export default (updatedID) => {
  // const { audio } = store.getState().editor
  // const id = getPreviousID(updatedID)
  // const text = (audio.wordID_to_text && audio.wordID_to_text[id])
  // // console.log({
  // //   id,
  // //   text,
  // //   sounds:  audio.sounds[text]
  // // })
  // return audio.sounds[text]
}
