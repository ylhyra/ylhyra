@import "reset.styl";

serif() {
  font-family: /*"Crimson",*/ Georgia, serif;
  letter-spacing: 0.005em;
}
sans_serif() {
  font-family: /*Inter V,*/ Helvetica, -apple-system, BlinkMacSystemFont, sans-serif;
  letter-spacing: 0;
}

input, textarea {
  color: black
  sans_serif()
}

body {
  sans_serif()
}
@import "button.styl";
@import "flashcards/*.styl";
@import "editor/*.styl";
